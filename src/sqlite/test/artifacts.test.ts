import { afterEach,expect,test } from "bun:test";import{mkdtemp,mkdir,rm,symlink,writeFile}from"node:fs/promises";import{tmpdir}from"node:os";import path from"node:path";import{LocalArtifactStore}from"../src/artifacts";
let root="";afterEach(async()=>{if(root)await rm(root,{recursive:true,force:true})});
test("loads canonical job artifacts and verifies flags",async()=>{root=await mkdtemp(path.join(tmpdir(),"artifacts-"));await mkdir(path.join(root,"jobs","job","interview-prep"),{recursive:true});await writeFile(path.join(root,"jobs","job","job-description.md"),"description");await writeFile(path.join(root,"jobs","job","interview-prep","general.md"),"prep");const store=new LocalArtifactStore(root);expect((await store.verify({jobs:[{id:"job",hasJobDescription:true,hasInterviewPrep:true}]})).ok).toBe(true)});
test("rejects unsafe artifact ids",async()=>{root=await mkdtemp(path.join(tmpdir(),"artifacts-"));expect(()=>new LocalArtifactStore(root).jobDescription("../escape")).toThrow("Invalid artifact id")});
test("does not follow symlinked markdown artifacts",async()=>{root=await mkdtemp(path.join(tmpdir(),"artifacts-"));const outside=path.join(root,"outside.md"),description=path.join(root,"jobs","job","job-description.md");await mkdir(path.dirname(description),{recursive:true});await writeFile(outside,"private");await symlink(outside,description);const store=new LocalArtifactStore(root);expect(await store.jobDescriptionExists("job")).toBe(false);await expect(store.jobDescription("job")).rejects.toThrow("not a regular file")});
