import { afterEach, expect, test } from "bun:test";
import { openDatabase,migrateDatabase } from "../database";
import { SqliteScoutCompanyImportStore } from "../scout-company-import-store";
import { SqliteScoutRunStore } from "../scout-run-store";
import { importScoutCompany } from "../../core/scout-company-import";
const databases:ReturnType<typeof openDatabase>[]=[];afterEach(()=>databases.splice(0).forEach(db=>db.close()));
function setup(){const db=openDatabase(":memory:");databases.push(db);migrateDatabase(db);importScoutCompany({id:"company-1",name:"Example Company",active:true,sources:[{key:"official",type:"json",url:"https://careers.example.test/jobs",recordsPath:"jobs",fields:{id:"id",title:"title",url:"url"}}]},new SqliteScoutCompanyImportStore(db));return new SqliteScoutRunStore(db);}
test("full-run creation is singleton guarded and outbox jobs carry immutable configuration",()=>{const store=setup();const first=store.startOrReuse(20,5,"2026-01-01T00:00:00Z");const second=store.startOrReuse(99,9,"2026-01-01T00:01:00Z");expect(first.created).toBeTrue();expect(second).toEqual({run:first.run,created:false});const jobs=store.pendingJobs(20);expect(jobs).toHaveLength(1);expect(jobs[0]?.sources[0]?.key).toBe("official");});
test("terminal redelivery is idempotent and historical positions are stable",()=>{const store=setup();const run=store.startOrReuse(20,5,"2026-01-01T00:00:00Z").run;const job=store.pendingJobs(1)[0]!;const result={companyId:job.companyId,configurationVersionId:job.configurationVersionId,positions:[{sourceKey:"official",externalId:"role-1",canonicalUrl:"https://careers.example.test/jobs/1",title:"Systems Gardener",location:"Remote",description:null,provenance:{sourceKey:"official",sourceUrl:"https://careers.example.test/jobs",description:"none" as const}}],sources:[{sourceKey:"official",status:"succeeded_with_results" as const,positions:[] as never[],attempts:[{adapter:"json" as const,stage:"listing",requestCount:1,responseCount:1,candidateCount:1,acceptedCount:1,rejectedCount:0,validationStatus:"verified" as const,startedAt:"2026-01-01T00:00:00Z",completedAt:"2026-01-01T00:00:01Z",diagnostics:[]}]}]};result.sources[0]!.positions=result.positions as never[];store.commitResult(job,result,"2026-01-01T00:00:01Z");store.commitResult(job,result,"2026-01-01T00:00:02Z");expect(store.get(run.id)?.status).toBe("completed");expect(store.positions(run.id,{offset:0,limit:20}).items).toHaveLength(1);});
